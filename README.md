1. Clone or pull project
2. If using Visual Studio Code. In terminal run "mvn clean install" to install dependencies
3. If using Visual Studio Code. In terminal run "mvn spring-boot:run"
4. Server run on por 8081, change port in application.properties. Test in Postman with GET http://localhost:8081/hello. Output "Hello, World!"
